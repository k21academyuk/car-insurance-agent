name: Nightly Eval
on:
  schedule: [{ cron: "0 2 * * *" }]
  workflow_dispatch:
jobs:
  eval:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo "Run RAGAS + agent eval here"
